resources:
  repositories:
    - repository: ESODevTools
      type: git
      name: ESODevTools
name: $(BuildID)
trigger:
  - master
pr: none
variables:
  - group: TranslatorKey
jobs:
  - template: ci.yml@ESODevTools
